Feature: Bruw
  Bruw commands allows to

  Scenario: get current CLI version
    When I run `bruw version`
    Then the output should contain "0.1.0"

  Scenario: have decidim subcommand
    When I run `bruw decidim`
    Then the output should contain "help"
